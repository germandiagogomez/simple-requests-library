CXX = clang++

CXXFLAGS = -std=c++14 -Ideps/include

LIBSPREFIX = deps/lib
LIBS = $(LIBSPREFIX)/libboost_system.a $(LIBSPREFIX)/libdocopt_s.a

EXE = get_urls


: $(EXE).cpp |> $(CXX) $(CXXFLAGS) %f -o %o $(LIBS) |> %B

